<!DOCTYPE html>
<html>
  <head>
    <title>StripeProject</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%#= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application.bootstrap", "data-turbo-track": "reload" %>
    <%#= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= javascript_include_tag "/assets/application.js", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>

    <br>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <% if user_signed_in? %>
      <!-- Logged in as <strong><%#= current_user.email %></strong> -->
      <center><%= button_to "Logout User", destroy_user_session_path, method: :delete, class: "navbar-link"  %></center>
      <% else %>
        <%#= link_to "Sign up", new_user_registration_path, class: "navbar-brand" %> 
        <%= link_to "Login", new_user_session_path, class: "navbar-brand"  %>
      <% end %>
    </div>
    <%= yield %>
  </body>
</html>
